# Build artifacts
build/
cmake-build-debug/
cmake-build-release/

# MacOS files
.DS_Store

# Logs
*.log

# Generated plugin files
plugins/*.so
plugins/*.dll
plugins/*.dylib

# Pipeline configuration files (may contain sensitive data). Ignore the dir
# CONTENTS (pipelines/*) not the dir itself, so the template negation works.
pipelines/*
!pipelines/*.template.json

# Signaling files
signaling/
webrtc_client.html
WebRTC_Guide.md
.vscode/
.VSCodeCounter/

# clangd / editor cache
.cache/

# benchmark artifacts (large)
bench/models/
bench/clips/
bench/*.local
bench/samples/

# bench/metal build artifacts (keep .mm/.metal sources)
bench/metal/metal_va_validate
bench/metal/metal_coreml_infer
bench/metal/*.ppm
bench/metal/*.png
bench/.venv-yolo/
