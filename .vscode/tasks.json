{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build & Test zm-next",
			"type": "shell",
			"command": "mkdir -p build && cd build && cmake .. && make -j && ctest --output-on-failure",
			"group": "build",
			"problemMatcher": [
				"$gcc"
			],
			"isBackground": false
		}
	]
}